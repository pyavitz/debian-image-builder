From 9687de296647c9aad6c93edd44669df841d25513 Mon Sep 17 00:00:00 2001
From: Patrick Yavitz <pyavitz@gmail.com>
Date: Wed, 5 Mar 2025 08:15:24 -0500
Subject: [PATCH] Add ac2xx support

Signed-off-by: Patrick Yavitz <pyavitz@gmail.com>
---
 board/amlogic/ac2xx/Makefile |  7 +++++
 board/amlogic/ac2xx/ac2xx.c  | 53 ++++++++++++++++++++++++++++++++++++
 2 files changed, 60 insertions(+)
 create mode 100644 board/amlogic/ac2xx/Makefile
 create mode 100644 board/amlogic/ac2xx/ac2xx.c

diff --git a/board/amlogic/ac2xx/Makefile b/board/amlogic/ac2xx/Makefile
new file mode 100644
index 00000000000..6f89a3be493
--- /dev/null
+++ b/board/amlogic/ac2xx/Makefile
@@ -0,0 +1,7 @@
+# SPDX-License-Identifier: GPL-2.0+
+#
+# (C) Copyright 2025 BayLibre, SAS
+# Author: Neil Armstrong <narmstrong@baylibre.com>
+# Editor: flippy <flippy@sina.com>
+
+obj-y	:= ac2xx.o
diff --git a/board/amlogic/ac2xx/ac2xx.c b/board/amlogic/ac2xx/ac2xx.c
new file mode 100644
index 00000000000..f440a7123f4
--- /dev/null
+++ b/board/amlogic/ac2xx/ac2xx.c
@@ -0,0 +1,53 @@
+// SPDX-License-Identifier: GPL-2.0+
+/*
+ * Copyright (C) 2025 BayLibre, SAS
+ * Author: Neil Armstrong <narmstrong@baylibre.com>
+ * Editor: flippy <flippy@sina.com>
+ * Editor: Patrick Yavitz <pyavitz@gmail.com>
+ */
+
+#include <dm.h>
+#include <adc.h>
+#include <env.h>
+#include <init.h>
+#include <net.h>
+#include <asm/io.h>
+#include <asm/arch/boot.h>
+#include <asm/arch/sm.h>
+#include <asm/arch/eth.h>
+#include <asm/arch/boot.h>
+
+#define EFUSE_MAC_OFFSET	0
+#define EFUSE_MAC_SIZE		12
+#define MAC_ADDR_LEN		6
+
+int misc_init_r(void)
+{
+	u8 mac_addr[MAC_ADDR_LEN];
+	char efuse_mac_addr[EFUSE_MAC_SIZE], tmp[3];
+	ssize_t len;
+
+	if (!eth_env_get_enetaddr("ethaddr", mac_addr)) {
+		len = meson_sm_read_efuse(EFUSE_MAC_OFFSET,
+					  efuse_mac_addr, EFUSE_MAC_SIZE);
+		if (len != EFUSE_MAC_SIZE)
+			return 0;
+
+		/* MAC is stored in ASCII format, 1bytes = 2characters */
+		for (int i = 0; i < 6; i++) {
+			tmp[0] = efuse_mac_addr[i * 2];
+			tmp[1] = efuse_mac_addr[i * 2 + 1];
+			tmp[2] = '\0';
+			mac_addr[i] = hextoul(tmp, NULL);
+		}
+
+		if (is_valid_ethaddr(mac_addr))
+			eth_env_set_enetaddr("ethaddr", mac_addr);
+		else
+			meson_generate_serial_ethaddr();
+
+		eth_env_get_enetaddr("ethaddr", mac_addr);
+	}
+
+	return 0;
+}
-- 
2.39.5

