# Rockchip

flash_uboot(){
if [[ "$BOARD" == "odroidm1" ]]; then
	dd if=files/boot/odroidm1-u-boot.bin of="${IMAGE_LOOP_DEV_UBOOT}" conv=fsync;
else
	dd if=${OUTPUT}/idbloader.bin of="${IMAGE_LOOP_DEV}" seek=64;
	dd if=${OUTPUT}/u-boot.itb of="${IMAGE_LOOP_DEV}" seek=16384;
fi
}
